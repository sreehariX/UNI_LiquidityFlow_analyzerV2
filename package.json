{
  "name": "uni_liquidity_flow_analyzer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ uni_liquidity_flow_analyzer",
    "create-local": "graph create --node http://localhost:8020/ uni_liquidity_flow_analyzer",
    "remove-local": "graph remove --node http://localhost:8020/ uni_liquidity_flow_analyzer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 uni_liquidity_flow_analyzer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.84.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
